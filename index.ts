const names: string[] = ["Shareef", "Shafqat", "Shakeel", "Shafeeq", "Shameem"];

for (const name of names) {
    console.log(name);
}

console.log('-' .repeat(100));
// Second Way

for (let i=0; i< names.length; i++)
{
    console.log(names[i]);
}
export{};